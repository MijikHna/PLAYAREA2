const config = {
  urls: {
    base: `http://${window.location.host}`,
    allCars: `http://${window.location.host}/apps/various/test-rpc-1/all`,
    getCar: `http://${window.location.host}/apps/various/test-rpc-1`,
    createCar: `http://${window.location.host}/apps/various/test-rpc-1/create`,
  },
};

export default { config };
